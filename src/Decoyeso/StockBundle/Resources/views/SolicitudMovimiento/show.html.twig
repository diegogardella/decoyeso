<h1>SolicitudMovimiento</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Fechahoracreado</th>
            <td>{{ entity.fechaHoraCreado|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Fechahorareserva</th>
            <td>{{ entity.fechaHoraReserva|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Fechahoracierre</th>
            <td>{{ entity.fechaHoraCierre|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Estado</th>
            <td>{{ entity.estado }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('solicitudmovimiento') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('solicitudmovimiento_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('solicitudmovimiento_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
